package domParseDemo;
import org.w3c.dom.*;

import javax.xml.XMLConstants;
import javax.xml.parsers.*;
import javax.xml.validation.Schema;
import javax.xml.validation.SchemaFactory;
import javax.xml.validation.Validator;

import java.io.*;
public class readXMLfile {
/*
	//DocumentBuilderFactory factory = DocumentBuilderFactory.newInstance();
	//DocumentBuilder builder = factory.newDocumentBuilder();
	
	//Document document = builder.parse(new File( "F://Hadoop Training//data set//Datasets//employee.xml" ));
	
	//Schema schema = null;
	try {
	  String language = XMLConstants.W3C_XML_SCHEMA_NS_URI;
	  SchemaFactory factory = SchemaFactory.newInstance(language);
	 // schema = factory.newSchema(new File(name));
	} catch (Exception e) {
	    e.printStackStrace();
	}
	Validator validator = schema.newValidator();
	validator.validate(new DOMSource(document));
	
	Element root = document.getDocumentElement();
	
	element.getAttribute("employee") ;    //returns specific attribute
	element.getAttributes();    
	
	node.getElementsByTagName("subElementName") //returns a list of sub-elements of specified name
	node.getChildNodes()   */                      //returns a list of all child nodes
}
